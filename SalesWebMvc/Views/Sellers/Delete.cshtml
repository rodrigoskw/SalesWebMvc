@model SalesWebMvc.Models.Seller
@{
    ViewData["Title"] = "Delete";
}

<h2>@ViewData["Title"]</h2>

<h3>Tem certeza que deseja deletar ?</h3>

<div>
    <h4>Seller</h4>
    <hr />
    <dl>
        <dt>
            @Html.DisplayNameFor(vendedor => vendedor.Name)
        </dt>
        <dt>
            @Html.DisplayFor(vendedor => vendedor.Name)
        </dt>
        <dt>
            @Html.DisplayNameFor(vendedor => vendedor.Email)
        </dt>
        <dt>
            @Html.DisplayFor(vendedor => vendedor.Email)
        </dt>
        <dt>
            @Html.DisplayNameFor(vendedor => vendedor.BirthDate)
        </dt>
        <dt>
            @Html.DisplayFor(vendedor => vendedor.BirthDate)
        </dt>
        <dt>
            @Html.DisplayNameFor(vendedor => vendedor.BaseSalary)
        </dt>
        <dt>
            @Html.DisplayFor(vendedor => vendedor.BaseSalary)
        </dt>
    </dl>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class=" btn btn-danger" />
        <a asp-action="Index">Voltar para a lista</a>
    </form>
</div>

